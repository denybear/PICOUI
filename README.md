# PICOUI
Simple UI manager in micropython for raspberry PICO. The goal is to accept user input-outputs through buttons/keypresses, rotary encoder, etc, and to output via neopixel leds. The program is just a main loop to manage all these IOs.
